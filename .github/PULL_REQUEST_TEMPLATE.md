## Summary

<!-- What does this change, and why? -->

## Testing

<!-- How did you verify it? Which Mac, macOS version, and display(s)? -->

## Checklist

- [ ] `xcodegen generate && xcodebuild -scheme thetoolbox -configuration Debug build` succeeds
- [ ] Build-setting / file changes were made in `project.yml`, not the generated `.xcodeproj`
- [ ] Updated the README if behavior or usage changed
